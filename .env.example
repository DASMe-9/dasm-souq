# DASM Souq — environment variables
# Backend API base (without trailing slash; /api suffix already included by the client)
NEXT_PUBLIC_API_URL=https://api.dasm.com.sa/api

# DASM Talk widget origin (injected for the "ابدأ محادثة" button on product cards)
NEXT_PUBLIC_TALK_URL=https://talk.dasm.com.sa
